# RustZK - AI Mandates

## Core Rules
- **Validation**: Every modification MUST be followed by executing `./scripts/ci.sh`. Any failure in the script counts as a task failure and must be rectified before proceeding.
- **Standards**: Adhere strictly to the rules defined in `.agent/workflows/development-rules.md`.
- **References**: Always consult `pyzk_ref/` for protocol and logic accuracy.
